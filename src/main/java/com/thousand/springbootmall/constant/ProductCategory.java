package com.thousand.springbootmall.constant;

//constant放常數
//ProductCategory存放商品分類 (必為大寫) (複數單字用底線區隔)
//enum的name方法(轉換成字串類別)
//enum的valueOf方法(查詢enum內有無符合值)
public enum ProductCategory {
    FOOD,
    CAR,
    E_BOOK
}
